# 贡献指南（CONTRIBUTING）

欢迎加入高考双修的开源大军！🚀 本项目 GPL-3.0（代码）+ CC BY-SA 4.0（内容），你的每一次贡献都会让"让卖课的干不下去"更近一步。

## 你能贡献什么

| 类型 | 说明 | 难度 |
| --- | --- | --- |
| **内容** | 写/改 Markdown：知识点、例题（标准+邪修）、口诀卡 | ⭐ 不需要会编程 |
| **纠错** | 发现错误 → 提 Issue（或直接 PR 修改） | ⭐ |
| **代码** | Vue3 前端、内容管线脚本 | ⭐⭐⭐ |
| **设计** | 主题样式、图标、动效 | ⭐⭐ |

## 贡献内容的流程

1. **Fork 仓库** → clone 到本地；
2. 在 `content/` 下新建或修改 Markdown，格式严格参考 `content/_schema.md`：
   - 知识点：`type: knowledge`；
   - 例题：`type: example`，正文用 `## 题目` / `## 标准解法` / `## 邪修解法` 分节；
   - 有邪修必须带 `applicability`（适用范围）+ `risk`（风险提示）+ `fit`（适用特征列表）；
   - 真题引用注明"年份+卷别+题号"，自编内容标注"自编"；
3. 本地验证：
   ```bash
   npm install
   npm run content   # 无 [warn] 输出
   npm run build     # 构建成功
   npm run preview   # 浏览器检查页面效果
   ```
4. 提交 PR，说明改动内容与依据（如教材页码/考纲条目/真题出处）。

## 内容质量红线

- ❌ 不搬运付费资料、教辅原文（尊重版权，全部原创编写）；
- ❌ 不写没有把握的"邪修"——宁可没有，不可坑人；
- ✅ 邪修必须写清边界：只在什么题型下成立、大题为何不能用；
- ✅ 标准解法必须完整可写过程（这是学生考试要用的）。

## 纠错怎么提

页面上的「纠错反馈」按钮会自动生成 Issue，内容包括：

- 节点路径与标题；
- 问题描述（最好附正确内容或出处）；
- 我们会尽快修正并标注"已复核"。

## 代码贡献规范

- Vue 3 + TypeScript + `<script setup>`，样式走 `src/styles/theme.css` 的 CSS 变量；
- 内容管线在 `scripts/build-content.mjs`，改完跑 `npm run content` + `npm run build` + `node scripts/smoke.mjs`；
- 提交信息用中文描述，如 `feat: 新增数学导数恒成立例题`。

## 路线图（欢迎认领）

- [ ] 数学：导数压轴、立体几何例题扩充
- [ ] 物理：电学实验图像题、压轴计算题模板
- [ ] 化学：有机推断大题拆解、工艺流程题
- [ ] 生物：大题答题句式模板（长句表达）
- [ ] 全站：学习进度统计、每日一题、错题导出
- [ ] 部署：EdgeOne Pages 上线 + 自定义域名

有问题直接在 Issue 里 @ 维护者，或 PR 里留言。冲！😈
